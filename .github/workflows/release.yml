name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to build (tag/branch/SHA)"
        required: true
        default: "main"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write  # needed to create GitHub Releases

jobs:
  build:
    name: Build ${{ matrix.mc_version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        mc_version: ["1.21.1", "1.21.4", "26.1.2"]
    outputs:
      mod_version: ${{ steps.version.outputs.mod_version }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          # Install both Java 21 (for 1.21.x) and Java 25 (for MC 26.1.2);
          # Gradle's per-module toolchain picks the right one.
          java-version: |
            21
            25
      - uses: gradle/actions/setup-gradle@v6
      - name: Build
        run: ./gradlew :neoforge-${{ matrix.mc_version }}:build
      - name: Read mod version
        id: version
        run: |
          MOD_VERSION=$(grep '^mod_version=' gradle.properties | cut -d'=' -f2)
          echo "mod_version=$MOD_VERSION" >> "$GITHUB_OUTPUT"
      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: jars-${{ matrix.mc_version }}
          path: neoforge-${{ matrix.mc_version }}/build/libs/*.jar
          retention-days: 7

  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v6
      - name: Download all jars
        uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: jars-*
          merge-multiple: true
      - name: List artifacts
        run: find dist -type f
      - name: Extract this version's changelog section
        # Pulls "## [<mod_version>] ..." from CHANGELOG.md into RELEASE_NOTES.md;
        # falls back to a 1-line placeholder if the version isn't documented.
        run: |
          MOD_VERSION='${{ needs.build.outputs.mod_version }}'
          awk -v marker="## [$MOD_VERSION]" '
            in_section && /^## \[/ { exit }
            index($0, marker) == 1 { in_section = 1 }
            in_section { print }
          ' CHANGELOG.md > RELEASE_NOTES.md
          if [ ! -s RELEASE_NOTES.md ]; then
            printf '## %s\n\nAutomated release. See [CHANGELOG.md](https://github.com/%s/blob/main/CHANGELOG.md) for full notes.\n' \
              "$MOD_VERSION" "${{ github.repository }}" > RELEASE_NOTES.md
          fi
          echo "--- RELEASE_NOTES.md ---"
          cat RELEASE_NOTES.md
      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          gh release create "$TAG" \
            --title "$TAG" \
            --notes-file RELEASE_NOTES.md \
            dist/*.jar

  publish:
    name: Publish ${{ matrix.mc_version }} to CurseForge and Modrinth
    runs-on: ubuntu-latest
    needs: build
    strategy:
      fail-fast: false
      matrix:
        mc_version: ["1.21.1", "1.21.4", "26.1.2"]
    steps:
      - uses: actions/checkout@v6
      - name: Download jar
        uses: actions/download-artifact@v8
        with:
          name: jars-${{ matrix.mc_version }}
          path: dist
      - name: Drop sources jars before publish
        # CurseForge rejected the v0.2.0-alpha.1 upload because the sources
        # jar (only .java files) was treated as a primary file alongside the
        # real mod jar. Sources belong on GitHub Releases, not on CurseForge
        # / Modrinth — strip them out before mc-publish runs.
        run: |
          ls -la dist/
          rm -f dist/*-sources.jar
          echo "after sources removal:"
          ls -la dist/
      - name: Extract this version's changelog section
        # Same logic as the github-release job — extract "## [<mod_version>]"
        # from CHANGELOG.md into RELEASE_NOTES.md, with a 1-line fallback if
        # the version isn't documented yet.
        run: |
          MOD_VERSION='${{ needs.build.outputs.mod_version }}'
          awk -v marker="## [$MOD_VERSION]" '
            in_section && /^## \[/ { exit }
            index($0, marker) == 1 { in_section = 1 }
            in_section { print }
          ' CHANGELOG.md > RELEASE_NOTES.md
          if [ ! -s RELEASE_NOTES.md ]; then
            printf 'Automated release. See https://github.com/%s/blob/main/CHANGELOG.md for full notes.\n' \
              '${{ github.repository }}' > RELEASE_NOTES.md
          fi
      # Publishes to whichever platforms have secrets/vars configured.
      # Missing CURSEFORGE_TOKEN / MODRINTH_TOKEN cause the action to skip
      # that platform silently rather than fail the whole job.
      - name: Publish
        uses: Kir-Antipov/mc-publish@v3
        with:
          curseforge-id: ${{ vars.CURSEFORGE_PROJECT_ID }}
          curseforge-token: ${{ secrets.CURSEFORGE_TOKEN }}
          modrinth-id: ${{ vars.MODRINTH_PROJECT_ID }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          files: dist/*.jar
          name: "Lockers ${{ needs.build.outputs.mod_version }} for ${{ matrix.mc_version }}"
          version: "${{ needs.build.outputs.mod_version }}+${{ matrix.mc_version }}"
          version-type: release
          loaders: neoforge
          game-versions: ${{ matrix.mc_version }}
          changelog-file: RELEASE_NOTES.md
